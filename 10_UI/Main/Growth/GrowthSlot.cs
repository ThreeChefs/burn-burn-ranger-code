using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GrowthSlot : BaseSlot
{
    
    GrowthInfo _growthInfo;

    public void SetSlot(SlotInfo slotInfo, GrowthInfo growthInfo )
    {
        SetSlot(slotInfo);
    }
}
